<UserControl x:Class="SystemeCaisse.UI.Controls.KpiCard"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="100" d:DesignWidth="200"
             x:Name="Root">
    <Border Background="White" CornerRadius="12" Padding="20">
        <Border.Effect>
            <DropShadowEffect BlurRadius="15" ShadowDepth="2" Opacity="0.1" Color="#000"/>
        </Border.Effect>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            
            <StackPanel Grid.Column="0">
                <TextBlock Text="{Binding Title, ElementName=Root}" Foreground="#7F8C8D" FontSize="13" FontWeight="SemiBold" Margin="0,0,0,5"/>
                <TextBlock Text="{Binding Value, ElementName=Root}" Foreground="#2C3E50" FontSize="24" FontWeight="Bold"/>
                
                <StackPanel Orientation="Horizontal" Margin="0,5,0,0">
                    <TextBlock Text="{Binding Trend, ElementName=Root}" FontSize="12" FontWeight="SemiBold">
                        <TextBlock.Style>
                            <Style TargetType="TextBlock">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsPositiveTrend, ElementName=Root}" Value="True">
                                        <Setter Property="Foreground" Value="#27AE60"/>
                                        <Setter Property="Text" Value="▲"/>
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding IsPositiveTrend, ElementName=Root}" Value="False">
                                        <Setter Property="Foreground" Value="#C0392B"/>
                                        <Setter Property="Text" Value="▼"/>
                                    </DataTrigger>
                                    <!-- Fallback/Neutral if needed can be handled via code logic for hiding -->
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>
                    <TextBlock Text="{Binding TrendLabel, ElementName=Root}" FontSize="12" Foreground="#95A5A6" Margin="5,0,0,0"/>
                </StackPanel>
            </StackPanel>

            <!-- Icon Placeholder -->
            <Border Grid.Column="1" Width="40" Height="40" CornerRadius="20" Background="{Binding IconBackground, ElementName=Root}">
                <TextBlock Text="{Binding Icon, ElementName=Root}" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="White" FontSize="18"/>
            </Border>
        </Grid>
    </Border>
</UserControl>
