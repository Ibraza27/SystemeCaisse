<UserControl x:Class="SystemeCaisse.UI.Controls.SimplePieChart"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300"
             x:Name="ChartRoot">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"/> <!-- Chart -->
            <ColumnDefinition Width="1*"/> <!-- Legend -->
        </Grid.ColumnDefinitions>

        <!-- Chart Container -->
        <Canvas x:Name="DrawingCanvas" Grid.Column="0" HorizontalAlignment="Center" VerticalAlignment="Center" Width="200" Height="200"/>

        <!-- Legend -->
        <ItemsControl Grid.Column="1" ItemsSource="{Binding LegendItems, ElementName=ChartRoot}" VerticalAlignment="Center" Margin="10,0,0,0">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal" Margin="0,4">
                        <Border Width="12" Height="12" Background="{Binding ColorBrush}" CornerRadius="2" Margin="0,0,8,0"/>
                        <StackPanel>
                            <TextBlock Text="{Binding Label}" FontSize="11" FontWeight="SemiBold" Foreground="#555"/>
                            <TextBlock Text="{Binding Percentage, StringFormat={}{0:P0}}" FontSize="10" Foreground="#999"/>
                        </StackPanel>
                    </StackPanel>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </Grid>
</UserControl>
