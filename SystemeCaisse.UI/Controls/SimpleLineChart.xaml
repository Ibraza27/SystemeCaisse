<UserControl x:Class="SystemeCaisse.UI.Controls.SimpleLineChart"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="200" d:DesignWidth="500"
             x:Name="LineChartRoot"
             SizeChanged="UserControl_SizeChanged">
    <Grid>
        <Canvas x:Name="ChartCanvas" Margin="30,10,10,30" ClipToBounds="True">
            <!-- Axes and Grid lines will be drawn here -->
        </Canvas>
        
        <!-- Y Axis Labels Container -->
        <Canvas x:Name="AxisYCanvas" Margin="0,10,0,30" Width="30" HorizontalAlignment="Left"/>
        
        <!-- X Axis Labels Container -->
        <Canvas x:Name="AxisXCanvas" Margin="30,0,10,0" Height="30" VerticalAlignment="Bottom"/>
    </Grid>
</UserControl>
