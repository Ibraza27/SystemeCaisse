<Window x:Class="SystemeCaisse.UI.Views.DiscountWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:SystemeCaisse.UI.ViewModels"
        mc:Ignorable="d"
        Title="Remise manuelle" SizeToContent="WidthAndHeight"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        Background="#F5F5F7">
    <Grid Margin="20">
        <StackPanel>
            <TextBlock Text="Appliquer la remise sur :" FontSize="18" FontWeight="Bold" Margin="0,0,0,15"/>
            
            <RadioButton Content="Le total de la vente" IsChecked="{Binding ApplyToCart}" FontSize="16" Margin="0,0,0,10"/>
            <RadioButton Content="Un article spécifique" IsChecked="{Binding ApplyToItem}" FontSize="16" Margin="0,0,0,10"/>
            
            <ComboBox ItemsSource="{Binding AvailableItems}" SelectedItem="{Binding SelectedItem}" 
                      IsEnabled="{Binding ApplyToItem}" Margin="20,0,0,20" Padding="5">
                <ComboBox.ItemTemplate>
                    <DataTemplate>
                        <TextBlock>
                            <Run Text="{Binding ProduitNom}"/>
                            <Run Text=" - "/>
                            <Run Text="{Binding PrixUnitaire, StringFormat={}{0:C}}"/>
                        </TextBlock>
                    </DataTemplate>
                </ComboBox.ItemTemplate>
            </ComboBox>

            <TextBlock Text="Type de remise :" FontSize="18" FontWeight="Bold" Margin="0,0,0,15"/>
            <StackPanel Orientation="Horizontal" Margin="0,0,0,20">
                <RadioButton Content="Pourcentage (%)" IsChecked="{Binding IsPercentage}" FontSize="16" Margin="0,0,20,0"/>
                <RadioButton Content="Montant fixe (€)" IsChecked="{Binding IsAmount}" FontSize="16"/>
            </StackPanel>

            <Grid Margin="0,0,0,30">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="Valeur :" FontSize="16" VerticalAlignment="Center" Margin="0,0,10,0"/>
                <TextBox Grid.Column="1" Text="{Binding Value, UpdateSourceTrigger=PropertyChanged}" FontSize="20" FontWeight="Bold" Padding="5"/>
                <TextBlock Grid.Column="2" Text="%" FontSize="20" VerticalAlignment="Center" Margin="10,0,0,0" 
                           Visibility="{Binding IsPercentage, Converter={StaticResource BoolToVis}}"/>
                <TextBlock Grid.Column="2" Text="€" FontSize="20" VerticalAlignment="Center" Margin="10,0,0,0" 
                           Visibility="{Binding IsAmount, Converter={StaticResource BoolToVis}}"/>
            </Grid>

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Button Content="Annuler" Command="{Binding CancelCommand}" Margin="0,0,10,0" Padding="10" Background="#E0E0E0" FontSize="16"/>
                <Button Grid.Column="1" Content="Appliquer" Command="{Binding ApplyCommand}" Padding="10" Background="#2E7D32" Foreground="White" FontSize="20" FontWeight="Bold"/>
            </Grid>
        </StackPanel>
    </Grid>
</Window>
