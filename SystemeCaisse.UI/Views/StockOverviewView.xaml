<UserControl x:Class="SystemeCaisse.UI.Views.StockOverviewView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="1000">
    <Grid Background="White">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/> <!-- Top Stats -->
            <RowDefinition Height="Auto"/> <!-- Filters -->
            <RowDefinition Height="*"/>    <!-- Data Grid -->
            <RowDefinition Height="Auto"/> <!-- Bottom Actions -->
        </Grid.RowDefinitions>

        <!-- DEBUG: Check DataContext -->
        <TextBlock Text="{Binding Produits.Count, StringFormat='DEBUG: Loaded {0} items'}" Foreground="Red" HorizontalAlignment="Right" VerticalAlignment="Top" Panel.ZIndex="999"/>

        <!-- 1. Top Stats Banner -->
        <Border Grid.Row="0" Background="#F5F5F5" Padding="20" Margin="0,0,0,10" BorderBrush="#E0E0E0" BorderThickness="0,0,0,1">
            <UniformGrid Columns="4">
                <StackPanel>
                    <TextBlock Text="Valeur totale stock :" FontSize="12" Foreground="#666"/>
                    <TextBlock Text="{Binding ValeurTotaleStock, StringFormat={}{0:C}}" FontSize="20" FontWeight="Bold" Foreground="#2E7D32"/>
                </StackPanel>
                <StackPanel>
                    <TextBlock Text="Nombre de produits :" FontSize="12" Foreground="#666"/>
                    <TextBlock Text="{Binding Produits.Count}" FontSize="20" FontWeight="Bold" Foreground="#333"/>
                </StackPanel>
                <StackPanel>
                    <TextBlock Text="Produits en alerte :" FontSize="12" Foreground="#666"/>
                    <TextBlock Text="{Binding NombreAlertes}" FontSize="20" FontWeight="Bold" Foreground="#F57F17"/>
                </StackPanel>
                <StackPanel>
                    <TextBlock Text="Produits en rupture :" FontSize="12" Foreground="#666"/>
                    <!-- Binding needed for Rupture count -->
                    <TextBlock Text="{Binding NombreRuptures}" FontSize="20" FontWeight="Bold" Foreground="#D32F2F"/>
                </StackPanel>
            </UniformGrid>
        </Border>

        <!-- 2. Filters Bar -->
        <Grid Grid.Row="1" Margin="10,0,10,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="300"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBlock Text="Rechercher :" VerticalAlignment="Center" Margin="0,0,10,0"/>
            <TextBox Grid.Column="1" Text="{Binding FilterSearch, UpdateSourceTrigger=PropertyChanged}" VerticalAlignment="Center" Height="25" Padding="5,0"/>

            <CheckBox Grid.Column="2" Content="En alerte seulement" IsChecked="{Binding FilterAlertOnly}" VerticalAlignment="Center" Margin="20,0,0,0"/>
            <CheckBox Grid.Column="3" Content="En rupture seulement" IsChecked="{Binding FilterRuptureOnly}" VerticalAlignment="Center" Margin="20,0,0,0"/>
            
            <Button Grid.Column="5" Content="🔄 Rafraîchir" Command="{Binding LoadCommand}" Padding="10,5" Background="White" BorderBrush="#CCC"/>
        </Grid>

        <!-- 3. Main DataGrid -->
        <DataGrid Grid.Row="2" ItemsSource="{Binding OverviewProductsView}" AutoGenerateColumns="False" IsReadOnly="True"
                  GridLinesVisibility="Horizontal" Background="White" RowHeight="35"
                  HeadersVisibility="Column" Margin="10">
            <DataGrid.Resources>
                <Style TargetType="DataGridRow">
                    <Setter Property="Background" Value="#E8F5E9"/> <!-- Default Light Green as requested -->
                    <Style.Triggers>
                        <!-- Alternate row color or logic could be added here -->
                        <Trigger Property="IsSelected" Value="True">
                             <Setter Property="Background" Value="#C8E6C9"/>
                             <Setter Property="Foreground" Value="Black"/>
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </DataGrid.Resources>
            
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="50"/>
                <DataGridTextColumn Header="Code-barres" Binding="{Binding CodeBarre}" Width="120"/>
                <DataGridTextColumn Header="Nom du produit" Binding="{Binding Nom}" Width="*"/>
                <DataGridTextColumn Header="Stock" Binding="{Binding StockActuel}" Width="80" FontWeight="Bold"/>
                <DataGridTextColumn Header="Seuil" Binding="{Binding StockAlerte}" Width="80"/>
                <DataGridTextColumn Header="Valeur" Binding="{Binding ValeurStock, StringFormat={}{0:C}}" Width="100"/>
                
                <DataGridTemplateColumn Header="État" Width="100">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <TextBlock x:Name="txtState" Text="OK" Foreground="Green" FontWeight="Bold" HorizontalAlignment="Center"/>
                            <DataTemplate.Triggers>
                                <DataTrigger Binding="{Binding IsAlert}" Value="True">
                                    <Setter TargetName="txtState" Property="Text" Value="ALERTE"/>
                                    <Setter TargetName="txtState" Property="Foreground" Value="Red"/>
                                </DataTrigger>
                                <DataTrigger Binding="{Binding StockActuel}" Value="0">
                                     <Setter TargetName="txtState" Property="Text" Value="RUPTURE"/>
                                     <Setter TargetName="txtState" Property="Foreground" Value="DarkRed"/>
                                </DataTrigger>
                            </DataTemplate.Triggers>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <DataGridTextColumn Header="Dernière entrée" Binding="{Binding LastEntryDate, StringFormat=dd/MM/yyyy}" Width="120"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- 4. Bottom Actions -->
        <StackPanel Grid.Row="3" Orientation="Horizontal" Margin="10">
            <Button Content="📊 Exporter état des stocks" Padding="10,5" Margin="0,0,10,0"/>
            <Button Content="📦 Générer commande fournisseur" Padding="10,5" Margin="0,0,10,0"/>
            <Button Content="⚠️ Rapport des alertes" Padding="10,5"/>
        </StackPanel>
    </Grid>
</UserControl>
