<Window x:Class="SystemeCaisse.UI.Views.SplashScreen"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="SplashScreen" Height="400" Width="600"
        WindowStartupLocation="CenterScreen" WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        Topmost="True">
    
    <Border Background="White" CornerRadius="20">
        <Border.Effect>
            <DropShadowEffect Color="#888" BlurRadius="20" ShadowDepth="10" Opacity="0.5"/>
        </Border.Effect>
        <Grid Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="2*"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Logo -->
            <Image x:Name="LogoImage" Source="pack://application:,,,/Images/logo.png" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="20"/>

            <!-- Spinner / Loading Text -->
            <StackPanel Grid.Row="1" HorizontalAlignment="Center" Margin="0,20">
                <TextBlock Text="Chargement du système..." FontSize="16" Foreground="#666" HorizontalAlignment="Center"/>
                 <!-- Simplified Spinner (indeterminate progress bar) -->
                <ProgressBar IsIndeterminate="True" Width="200" Height="4" Margin="0,10,0,0" Foreground="#2E7D32"/>
            </StackPanel>

            <TextBlock Grid.Row="2" Text="v1.0.0 © Hippocampe Import-Export" FontSize="12" Foreground="#999" HorizontalAlignment="Center" Margin="0,10,0,0"/>
        </Grid>
    </Border>
</Window>
