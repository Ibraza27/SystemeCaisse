<Window x:Class="SystemeCaisse.UI.Views.CartItemSelectionWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Choisir un article" Height="500" Width="550"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        Background="#F5F5F7">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="🛍️ Sélectionner l'article" FontSize="20" FontWeight="Bold" Margin="0,0,0,15" HorizontalAlignment="Center"/>

        <ListBox x:Name="ItemsList" Grid.Row="1" BorderThickness="1" BorderBrush="#DDD" Grid.IsSharedSizeScope="True" 
                 ScrollViewer.HorizontalScrollBarVisibility="Disabled">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Grid Margin="10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" SharedSizeGroup="ImgCol"/>
                            <ColumnDefinition Width="*" SharedSizeGroup="NameCol"/>
                            <ColumnDefinition Width="Auto" SharedSizeGroup="PriceCol"/>
                        </Grid.ColumnDefinitions>
                        
                        <Border Width="40" Height="40" CornerRadius="4" Background="#F8F8F8" ClipToBounds="True" Margin="0,0,10,0">
                            <Grid>
                                <Image Source="{Binding Produit.FullImagePath, Converter={StaticResource PathToImage}}" Stretch="Uniform" Margin="2"/>
                                <TextBlock Text="📦" FontSize="14" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="#CCC"
                                           Visibility="{Binding Produit.FullImagePath, Converter={StaticResource InvertedNullToVis}}"/>
                            </Grid>
                        </Border>
                        
                        <TextBlock Grid.Column="1" Text="{Binding ProduitNom}" FontSize="16" FontWeight="SemiBold" TextWrapping="Wrap" VerticalAlignment="Center"/>
                        <TextBlock Grid.Column="2" Text="{Binding PrixUnitaire, StringFormat={}{0:C}}" FontSize="16" Foreground="#2E7D32" FontWeight="Bold" Margin="10,0,0,0" VerticalAlignment="Center"/>
                    </Grid>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
            <Button Content="Annuler" IsCancel="True" Padding="15,10" Foreground="Gray" Background="Transparent" BorderThickness="0" Margin="0,0,10,0"/>
            <Button Content="Sélectionner" Click="BtnValidate_Click" Padding="25,10" Background="#2E7D32" Foreground="White" FontWeight="Bold" FontSize="16"/>
        </StackPanel>
    </Grid>
</Window>
