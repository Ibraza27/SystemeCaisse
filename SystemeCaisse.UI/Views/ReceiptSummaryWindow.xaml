<Window x:Class="SystemeCaisse.UI.Views.ReceiptSummaryWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Fin de Vente" Height="650" Width="800"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        Topmost="True">
    
    <Border Background="White" CornerRadius="12" BorderBrush="#DDD" BorderThickness="1">
        <Border.Effect>
            <DropShadowEffect Color="#888" BlurRadius="20" ShadowDepth="10" Opacity="0.3"/>
        </Border.Effect>
        <Grid Margin="20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="350"/>
            </Grid.ColumnDefinitions>

            <!-- Left Side -->
            <StackPanel Grid.Column="0" VerticalAlignment="Center" Margin="0,0,20,0">
                <TextBlock Text="Vente Validée !" FontSize="32" FontWeight="Bold" Foreground="#2E7D32" HorizontalAlignment="Center" Margin="0,0,0,30"/>
                
                <Border Background="#F9F9F9" Padding="20" CornerRadius="8" Margin="0,0,0,30">
                    <StackPanel>
                        <TextBlock Text="MONNAIE À RENDRE" FontSize="14" Foreground="#666" HorizontalAlignment="Center"/>
                        <TextBlock x:Name="ChangeText" Text="0,00 €" FontSize="48" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,5"/>
                    </StackPanel>
                </Border>

                <StackPanel Orientation="Vertical">
                    <Button Content="🖨 IMPRIMER LE TICKET" Click="Print_Click" Height="60" Margin="0,0,0,10" Background="#2E7D32" Foreground="White" FontSize="18" FontWeight="Bold" Cursor="Hand"/>
                    <Button Content="💾 ENREGISTRER (XPS)" Click="Save_Click" Height="50" Margin="0,0,0,10" Background="#1976D2" Foreground="White" FontSize="16" Cursor="Hand"/>
                    <Button Content="Fermer (Nouvelle Vente)" Click="Close_Click" Height="50" Background="#EEE" Foreground="#333" FontSize="16" Cursor="Hand"/>
                </StackPanel>
            </StackPanel>

            <!-- Right Side: Preview -->
            <Border Grid.Column="1" Background="#F0F0F0" Padding="10" CornerRadius="8" BorderBrush="#CCC" BorderThickness="1">
                <FlowDocumentReader x:Name="DocReader" ViewingMode="Scroll" />
            </Border>
        </Grid>
    </Border>
</Window>
